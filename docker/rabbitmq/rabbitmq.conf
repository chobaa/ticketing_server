## RabbitMQ config for Prometheus metrics
##
## We need per-queue metrics (e.g., queue="payment.queue") to visualize
## ready vs unacked (in-flight) backlog accurately in Grafana.
prometheus.return_per_object_metrics = true

